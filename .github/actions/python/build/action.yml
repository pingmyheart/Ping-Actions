name: Python Build Action

runs:
  using: composite

  steps:
    - name: Python Build
      shell: bash
      run: |
        pip install build twine
        pip install pytest coverage
        python -m build
